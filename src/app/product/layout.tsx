import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "AquaBLADE Squeegee Kit | Shop Now - $39.95",
};

export default function ProductLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
